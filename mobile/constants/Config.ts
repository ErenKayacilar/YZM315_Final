// API Configuration
// IMPORTANT: Replace XXX with your PC's local IP address
// Find it via: ipconfig (Windows) or ifconfig (Mac/Linux)
// DO NOT use 'localhost' - it won't work on physical devices

export const Config = {
    // Production backend (Render)
    API_URL: 'https://yzm315-final.onrender.com/api',

    // Token storage keys
    TOKEN_KEY: 'auth_token',
    USER_KEY: 'auth_user',
    BIOMETRIC_KEY: 'biometric_enabled',
};
